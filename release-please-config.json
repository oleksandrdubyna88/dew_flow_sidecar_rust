{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "$note": [
    "How a release is decided here: release-please reads the conventional commits since the last",
    "tag, opens a pull request that bumps the version and writes RELEASES.md, and cutting the tag",
    "is what MERGING that pull request does. The release workflow already in this repository is",
    "what the tag then starts.",
    "",
    "include-component-in-tag is FALSE, deliberately and only here. This repository ships ONE",
    "product, so a component prefix distinguishes it from nothing; its release workflow triggers on",
    "`v*` and a prefixed tag would not start it at all — the release would silently produce nothing.",
    "The sibling repositories carry prefixes because four products share one tag namespace there."
  ],
  "$exclude": [
    "A COMMIT THAT ONLY TOUCHES CI IS NOT A RELEASE, and this is the mechanism rather than the",
    "reminder. From the action's own schema: \"Path of commits to be excluded from parsing. If ALL",
    "files from commit belong to one of the paths it will be skipped.\"",
    "",
    "Why it is here: `feat(ci): release-please runs on main` — a change to nothing but a workflow —",
    "duly proposed 0.3.0, a MINOR release of a product whose code had not moved. release-please reads",
    "the type literally, and `feat` means a feature. It then happened twice more in the sibling",
    "repositories on 2026-09-21, after the rule had been written down. A note that fails three times",
    "is not a rule, it is a wish; this is configuration, and it cannot be forgotten.",
    "",
    "EXACTLY ONE ENTRY, because two of the three this list started with excluded NOTHING.",
    "`CommitExclude.isRelevant` tests `file.indexOf(path + '/') === 0`, so an exclude path is a",
    "DIRECTORY PREFIX and a file path can never match itself — `release-please-config.json` and",
    "`.release-please-manifest.json` matched no file at all. Transcribed from",
    "src/util/commit-exclude.ts and run, rather than read and hoped. They are gone instead of left in",
    "looking like protection.",
    "",
    "AND `Release-As:` IS NOT AN ESCAPE HATCH from this, which this note used to claim.",
    "`excludeCommits()` filters the commit list BEFORE any release strategy sees it, so a dropped",
    "commit's footers are never parsed. If a CI-only change genuinely has to ship, put something",
    "outside `.github` in the same commit, or let it ride with the next real change.",
    "",
    "WHAT IS AND IS NOT COVERED, measured: a commit touching only `.github/**` is SKIPPED; one that",
    "also touches anything else is released. This repository's package is the root, so `.github` is",
    "the whole of what a pure CI commit can be — but in the siblings, where a setup commit added a",
    "version.txt inside each component, the same list does NOT cover that shape and the commit TYPE",
    "remains the only defence there.",
    "",
    "THE TRADE, said out loud: a change to `.github` that genuinely alters the product — a compiler",
    "flag, a build argument — is now skipped too. That is the smaller of the two errors: the other one",
    "inflates a version every single time anybody edits a workflow.",
    "",
    "`ci:` and `chore:` remain the right types for such a commit regardless. This stops the mistake",
    "from reaching a version number; it does not make the type meaningless."
  ],
  "exclude-paths": [
    ".github"
  ],
  "release-type": "rust",
  "include-component-in-tag": false,
  "changelog-path": "RELEASES.md",
  "packages": {
    ".": {}
  }
}
